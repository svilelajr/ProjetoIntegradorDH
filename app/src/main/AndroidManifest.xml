<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digitalhouse.moviewallet">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mw"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mw"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieWallet"
        android:usesCleartextTraffic="true"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.HomeScreen" />
        <activity android:name=".ui.activity.SearchScreenCategory" />
        <activity android:name=".ui.activity.DetailsScreen" />
        <activity android:name=".ui.activity.LoginScreen" />
        <activity android:name=".ui.activity.RegisterScreen" />
        <activity android:name=".ui.activity.ContextScreen" />
        <activity android:name=".ui.activity.SettingsScreen" />
        <activity android:name=".ui.activity.SearchScreen" />
        <activity android:name=".ui.activity.FavoritesScreen" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>