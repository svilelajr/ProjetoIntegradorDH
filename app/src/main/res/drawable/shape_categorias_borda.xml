<?xml version="1.0" encoding="utf-8"?>
    <shape
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape="line" >

        // A cor do background
        <solid android:color="@color/subtextox_app" />
        // Borda (tamanho e cor)
        <stroke android:width="1dp" android:color="@color/subtextox_app" />
 </shape>